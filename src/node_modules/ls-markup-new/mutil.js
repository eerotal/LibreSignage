/*
*  LibreSignage markup parser utility functions.
*/

module.exports.chk_lexeme_context = function(lexemes, expect) {
	for (let i in expect) {
		if (!(i in lexemes)) { return false; }
		if (!lexemes[i].matches(expect[i])) { return false; }
	}
	return true;
}
