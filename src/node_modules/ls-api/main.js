exports = {
	APIEndpoints: require('./APIEndpoints.js').APIEndpoints;
	APIInterface: require('./APIInterface.js').APIInterface;
	APIError:     require('./APIError.js').APIError;
	HTTPStatus:   require('./HTTPStatus.js').HTTPStatus;
}

// Add the various API classes to window.* for debugging.
if (typeof window !== 'undefined') { Object.assign(window, exports); }
