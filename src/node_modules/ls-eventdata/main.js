class EventData {
	constructor(data, then, except) {
		this.data = data;
		this.then = then != null ? then : () => {};
		this.except = except != null ? except: () => {};
	}

	get(key) { return this.data[key]; }
}
exports.EventData = EventData;
