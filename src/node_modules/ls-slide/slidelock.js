/*
*  Class for handling slide lock data.
*/
class SlideLock {
	constructor(session, expire) {
		this.session = session;
		this.expire = expire;
	}

	is_expired() { return Date.now()/1000 > this.expire; }
	get_session_data() { return this.session; }
}
exports.SlideLock = SlideLock;
